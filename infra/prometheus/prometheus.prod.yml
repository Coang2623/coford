global:
  scrape_interval: 10s

scrape_configs:
  # Backend Spring Boot chay nhu 1 container "backend" trong cung mang docker
  - job_name: coford-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ['backend:8080']
