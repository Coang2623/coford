global:
  scrape_interval: 10s

scrape_configs:
  # Backend Spring Boot chạy trên máy host (mvn spring-boot:run)
  - job_name: coford-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ['host.docker.internal:8080']
